# name: Build and Push Docker Image

# on:
#   push:
#     branches:
#       - master

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Login to DockerHub
#         run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

#       - name: Build Docker image
#         run: docker build -t 02230290namgay/bmi-app:latest .

#       - name: Push Docker image to DockerHub
#         run: docker push 02230290namgay/bmi-app:latest

name: Docker Build and Push
on: push
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Frontend
        run: |
         #refer the docker compose file and dockerfiles from the repository

      - name: Build and Push Backend
        run: |
         #refer the docker compose file and dockerfiles from the repository

      # Optional: DB image if not using Render's managed DB
      - name: Build and Push DB
        run: |
         #refer the docker compose file and dockerfiles from the repository